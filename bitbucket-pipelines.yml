image: node:20.12.1

pipelines:
  custom:
    staging:
      - step:
          name: Run build
          script:
            - npm install --force
            - npm run build NODE_ENV=development --mode staging
          artifacts:
            - dist/**
          caches:
            - node
      - step:
          name: Deploy to staging
          script:
            - echo "Deploying to staging environment"
            - pipe: atlassian/aws-s3-deploy:0.3.8
              variables:
                AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
                AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
                AWS_DEFAULT_REGION: $AWS_DEFAULT_REGION
                S3_BUCKET: $S3_BUCKET
                LOCAL_PATH: 'dist'
                ACL: 'public-read-write'
      - step:
          name: Run invalidations
          script:
            - aws-cloudfront-invalidate E1JQLAWU0IBM3T
